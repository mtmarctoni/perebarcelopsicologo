# AGENTS.md

## Branch Strategy

- **develop**: Staging branch. All feature/bugfix PRs must target `develop`.
- **main**: Production branch. Only receives PRs opened from `develop`. Never open a PR directly against `main`.
